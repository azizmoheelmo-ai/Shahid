# تعليمات دائمة لهذا المستودع (شاهد على الأداء الوظيفي)

## بروتوكول المراجعة بعد كل إضافة/تعديل

بعد أي إضافة ميزة أو تعديل أرى إنه بحجم أو خطورة تستحق ذلك (تقديري أنا — لا
أطبّقه على تعديل نص/تصميم بسيط لا يمسّ منطقًا أو بيانات)، أراجع العمل مراجعة
شاملة **قبل** اعتبار المهمة منتهية، بنفس روح "عصف ذهني كمبرمج شغّال في الأمن
السيبراني" المطلوبة مني سابقًا بهذا المشروع. أذكر نتيجة المراجعة للمستخدم
**دائمًا**، حتى لو لم أجد أي مشكلة (لا أكتفي بالصمت لمجرد أن كل شيء سليم).

**لا أحتفظ بسجل ملفات منفصل لنتائج المراجعات** — أذكر النتيجة بالمحادثة نفسها
فقط في كل مرة.

### نطاق المراجعة (شامل لكل الجوانب التالية):

1. **تسرّب بيانات بين المستخدمين** — الخطر الأكبر تاريخيًا بهذا المشروع.
   كل جدول تقريبًا له سياسة RLS "المسؤول يشوف الكل" بجانب سياسة المالك —
   فأي استعلام بشاشة شخصية (لا شاشة لوحة تحكم) **يجب** أن يحمل فلترة صريحة
   `.eq('user_id', currentUser.id)` أو `.eq('teacher_id', currentUser.id)`،
   ولا يعتمد على RLS وحدها. هذا بالضبط سبب أكثر من حادثة حقيقية بهذا المشروع.

2. **تعارض/تسرّب بين الشاشات (views)** — أي دالة `showX()` جديدة تعرض شاشة
   رئيسية يجب أن تستدعي `hideAllMainViews()` وتُظهر حاويتها الخاصة صراحةً،
   لا الاكتفاء بتبديل أقسام داخلية بافتراض أن الحاوية الأصلية ظاهرة أصلًا —
   هذا افتراض ينكسر متى استُدعيت الدالة من شاشة مختلفة (مثال حقيقي: حفظ شاهد
   من نموذج توثيق حصة برنامج كان يفشل بالانتقال لشاشة البرنامج).

3. **حذف بالخطأ / فقدان بيانات** — عند حذف أي سجل، تحقّق: هل تعتمد عليه
   سجلات أخرى (بشاشة أخرى) بحيث يصير حذفه يُبقيها "عالقة" بحالة غير قابلة
   للتصحيح؟ هل تُنظَّف أي مراجع/روابط مرتبطة (كملفات تخزين، أو حقول ربط
   بجداول أخرى) بدل تركها متيتّمة؟

4. **حالات السباق (races)** — أي callback غير متزامن (خصوصًا بعد
   `await`/`setTimeout`) يلتقط id أو سياقًا معيّنًا: تحقّق ماذا يحصل لو تغيّر
   السياق الفعلي (المستخدم انتقل لشاشة أخرى، أو عدّل بيانات من تبويب/جهاز
   آخر) قبل أن ينفّذ الـcallback فعليًا. الحل المعتاد هنا: تحقّق من متغيّر
   الحالة الحالية (مثل `currentProgramId`) قبل إعادة الرسم/الكتابة، أو إعادة
   قراءة أحدث نسخة من القاعدة مباشرة قبل حفظ تعديل بدل الاعتماد على نسخة
   محلية قديمة.

5. **XSS / حقن** — أي نص من المستخدم (أو من القاعدة) يُدرَج بـ`innerHTML`
   يجب أن يمرّ عبر `escapeHtml()` أولًا، بما في ذلك داخل سمات مثل `href`.

6. **الأزرار والعمليات الطويلة** — أي زر يشغّل عملية غير متزامنة (حفظ، حذف،
   تصدير، رفع صورة) يجب أن يتعطّل أثناء العملية (نمط `beginExportBusy`/
   `endExportBusy` الموحّد لأزرار التصدير، أو تعطيل مباشر لأزرار الحفظ/الحذف)
   لمنع نقرة مزدوجة تُنشئ سجلًا مكررًا أو تُنفّذ العملية مرتين.

7. **اكتمال النسخ الاحتياطية** — أي جدول جديد يُضاف للمشروع: تحقّق هل يجب
   إضافته لقائمتي exportFullBackup (app-07) وexportBackup (app-08)، ولقائمة
   الصور فيهما لو كان يحمل حقل صورة، ولترتيب استعادة CSV بدليل الاستعادة لو
   كان يُشار إليه من جدول آخر (foreign key) — الترتيب الخاطئ يكسر الاستعادة.

### أثناء التنفيذ وبعده

- عند اكتشاف خلل حقيقي، أُصلحه مباشرة (لا أكتفي بذكره) ما لم يكن يحتاج قرارًا
  من المستخدم (تغيير سلوك متعمَّد، أو نطاق أوسع من المتوقع).
- لكل إصلاح حقيقي: أكتب اختبار انحدار (regression test) عند الإمكان، وأتحقق
  يدويًا أنه يفشل فعلًا لو رجّعت الكود لحالته قبل الإصلاح (ثم أستعيد الإصلاح)
  — نفس الانضباط المتّبع بهذا المشروع أصلًا.
- أشغّل `node scripts/check-syntax.js` و`node --test tests/*.test.js` قبل أي
  commit، ولا أعتبر المهمة منتهية إلا بعد نجاح الاثنين.
